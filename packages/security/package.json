{
  "name": "@portal-fusion/security",
  "version": "1.0.0",
  "description": "Security and encryption for Portal Fusion",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"Linting security package...\" && exit 0",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@portal-fusion/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
